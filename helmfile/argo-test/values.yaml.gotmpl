resources:
  - apiVersion: v1
    kind: Pod
    metadata:
      name: simple-busybox
    spec:
      containers:
        - name: busybox-container
          image: busybox:latest
          command: ["sleep", "5600"] # Keeps the container running for an hour
